#include "main.h"
/**
 *mul - function that multiply 2 numbers
 *@a:the first parametre
 *@b:the second parameter
 *Return: result of the multiplication
 */
int mul(int a, int b);
{
int result;
result = a * b;
return (result);
}
